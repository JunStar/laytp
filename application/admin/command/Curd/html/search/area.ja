<div class="layui-inline">
                <select
                    name="row[{%field_name%}]"
                    id="{%field_name%}"
                    lay-filter="{%field_name%}"
                    placeholder="请选择{%field_comment%}"
                    linkage_area="true"
                    ajax-url="admin/ajax/area"
                    selected-id="{%selected_id%}"
                    is-province="{%is_province%}"
                    change-linkage-id="{%change_linkage_id%}">
                    {%options%}
                </select>
            </div>