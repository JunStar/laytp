<input type="text" name="row[{%field_name%}]" id="{%field_name%}" placeholder="请选择" class="layui-input selectPage"
                search-field="{%search_field%}"
                search-url="{%search_url%}"
                show-field="{%show_field%}"
                search-condition="LIKE"
                multiple="{%multiple%}"
                {%max%}>