<?php
/**
 * {%tableComment%}模型
 */
namespace {%modelNamespace%};

use model\Backend;

class {%modelClassName%} extends Backend
{
    //模型名
    protected $name = '{%modelName%}';

    //表名
    {%tableName%}

    //数组常量
    public $const = [
{%arrayConst%}
    ];

    //关联模型
    {%relationModel%}

    //获取数组常量的函数
    {%getArrayConstListFunction%}
}
