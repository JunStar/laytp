<div class="layTpUpload"
                data-name="{%field%}"
                data-accept="{%accept%}"{%width%}{%height%}
                data-multi="{%multi%}"
                data-max="{%max%}"
                data-dir="{%dir%}"
                data-url="{%url%}"
                data-mime="{%mime%}"
                data-size="{%size%}"
                data-layVerify="{%verify%}"
                data-uploaded="{{d.{%field%}}}"
            ></div>