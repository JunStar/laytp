<div class="layui-input-inline">
                <select
                    linkage="true"
                    lay-filter="{%field_name%}"
                    lay-verify="{%verify%}"
                    lay-verType="tips"
                    placeholder="请选择{%field_comment%}"
                    name="row[{%field_name%}]"
                    id="{%field_name%}"
                    table_name="{%table_name%}"
                    search_field="pid"
                    search_field_val="0"
                    selected_val="{%selected_val%}"
                    show_field="name"
                    left_field="{%left_field%}"
                    right_field="{%right_field%}"
                >
                    <option value="">请选择{%field_comment%}</option>
                </select>
            </div>